#include<stdio.h>
int main(){
    int a,width,length,height;
    printf("enter the width of rectangular prism:");
    scanf("%d",%width);
    printf("enter the length of rectangular prism:");
    scanf("%d",%length);
    printf("enter the height of rectangular prism:");
    scanf("%d",%height);
    
    a=2*(w*1+h*1+h*w);
    printf("area of rectangular prism is %d",a);
}
    
