/*structure employe that provides the following
a.information print and display empno,empname,address and age*/
#include<stdio.h>
int main(){
	int a,empno,empage;
	char empname[20],empaddress[40];
	
	do{
		printf("\n enter employe number:");
		scanf("%d",&empno);
		
		while(getchar()!='\n');
		printf("\n enter employe name:");
		gets(empname);
		
		printf("\n enter employe age:");
		scanf("%d",&empage);
		
		while(getchar()!='\n');
		printf("\n enter employe address:");
		gets(empaddress);
		
        printf("\n employe number is %d",empno);
        printf("\n employe name is %s",empname);
        printf("\n employe age is %d",empage);
        printf("\n employe address is %s",empaddress);
        
        (printf("\n 1").(add another detail \n 2).exit");
        printf("\n enter your choice:");
        scanf("%d",&a);
	}while(a==1);
}
